@model IEnumerable<DAL.Models.Thread>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Forum.ForumTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ThreadTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OwnerID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(Html.DisplayFor(modelItem => item.Forum.ForumTitle), "Index", "Post", new { id = item.ThreadID }, null)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ThreadTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OwnerID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ThreadID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ThreadID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ThreadID })
        </td>
    </tr>
}

</table>
